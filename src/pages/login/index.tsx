import React from "react";
import type { NextPage } from "next";
import { LoginForm, PatientData } from "components";

const Login: NextPage = () => {
  return <LoginForm />;
};

export default Login;
